# Backend base URL (server-side fetch + browser fetch both use this)
NEXT_PUBLIC_API_URL=http://localhost:4000
# Server-side only — used by route handlers proxying cookie-bound requests
API_URL=http://localhost:4000
# Ota-ona ko'radigan hisobot sayti (client/Astro). "Hisobotni ko'rish" va
# ota-onaga yuboriladigan xabardagi havola shu manzildan quriladi.
# Prod: https://natija.sodiqschool.uz — o'zgartirilsa admin QAYTA BUILD qilinadi
# (NEXT_PUBLIC_* build vaqtida kodga yoziladi).
NEXT_PUBLIC_CLIENT_URL=http://localhost:4321
